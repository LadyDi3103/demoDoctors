import { Routes } from "@angular/router";
import { MapScreenComponent } from "./screens/map-screen/map-screen.component";

export const MapsRouting:Routes = [
    {
        path:'',
        component: MapScreenComponent
    }
]
